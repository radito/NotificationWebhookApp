<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Webhook URLs are user supplied, so an HTTP exception cannot name one fixed host. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
